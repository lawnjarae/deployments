#!/bin/bash

# Download and place binary in JBoss. WAR_FILE_URL variable is specified as an environment variable in the BuildConfig
#curl -o $JBOSS_HOME/standalone/deployments/ROOT.war -O ${WAR_FILE_URL}

cat > $JBOSS_HOME/rhc_ose_license.txt << EOF
RHC Bootcamp Middleware License

This software is meets the standard set forth by the company and can be used within all deployment environments
EOF
